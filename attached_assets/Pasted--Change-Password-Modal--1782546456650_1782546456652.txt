<!-- ═══════════════════════════════════════════════════════════
     Change Password Modal
════════════════════════════════════════════════════════════ -->
<div
  id="modal-change-password"
  onclick="if(event.target===this)closeChangePassword()"
  style="
    display: none;
    position: fixed;
    inset: 0;
    background: rgba(0,0,0,0.65);
    z-index: 2000;
    align-items: center;
    justify-content: center;
  "
>
  <div style="
    background: var(--surface, #1a1a2e);
    border: 1px solid var(--border, #2a2a45);
    border-radius: 12px;
    padding: 2rem;
    width: 100%;
    max-width: 400px;
    margin: 1rem;
    box-sizing: border-box;
  ">

    <h3 style="margin: 0 0 1.25rem; color: var(--accent, #C9A84C); font-size: 1rem;">
      🔑 تغيير كلمة المرور / Change Password
    </h3>

    <div id="cp-error" style="
      display: none;
      background: rgba(224,90,90,0.12);
      border: 1px solid #e05a5a;
      color: #e05a5a;
      border-radius: 8px;
      padding: 0.5rem 0.75rem;
      font-size: 0.85rem;
      margin-bottom: 0.75rem;
    "></div>

    <div id="cp-success" style="
      display: none;
      background: rgba(46,204,138,0.12);
      border: 1px solid #2ecc8a;
      color: #2ecc8a;
      border-radius: 8px;
      padding: 0.5rem 0.75rem;
      font-size: 0.85rem;
      margin-bottom: 0.75rem;
    "></div>

    <label style="font-size: 0.8rem; color: var(--muted, #888899); display: block; margin-bottom: 0.3rem;">
      Current Password / كلمة المرور الحالية
    </label>
    <input
      type="password"
      id="cp-current"
      placeholder="••••••••"
      style="
        width: 100%;
        background: var(--bg, #0f0f1a);
        border: 1px solid var(--border, #2a2a45);
        color: var(--text, #e8e8f0);
        border-radius: 8px;
        padding: 0.6rem 0.75rem;
        font-size: 0.9rem;
        margin-bottom: 0.75rem;
        outline: none;
        direction: ltr;
        box-sizing: border-box;
      "
    >

    <label style="font-size: 0.8rem; color: var(--muted, #888899); display: block; margin-bottom: 0.3rem;">
      New Password / كلمة المرور الجديدة (min 8)
    </label>
    <input
      type="password"
      id="cp-new"
      placeholder="Min 8 characters"
      style="
        width: 100%;
        background: var(--bg, #0f0f1a);
        border: 1px solid var(--border, #2a2a45);
        color: var(--text, #e8e8f0);
        border-radius: 8px;
        padding: 0.6rem 0.75rem;
        font-size: 0.9rem;
        margin-bottom: 0.75rem;
        outline: none;
        direction: ltr;
        box-sizing: border-box;
      "
    >

    <label style="font-size: 0.8rem; color: var(--muted, #888899); display: block; margin-bottom: 0.3rem;">
      Confirm Password / تأكيد كلمة المرور
    </label>
    <input
      type="password"
      id="cp-confirm"
      placeholder="••••••••"
      style="
        width: 100%;
        background: var(--bg, #0f0f1a);
        border: 1px solid var(--border, #2a2a45);
        color: var(--text, #e8e8f0);
        border-radius: 8px;
        padding: 0.6rem 0.75rem;
        font-size: 0.9rem;
        margin-bottom: 1.25rem;
        outline: none;
        direction: ltr;
        box-sizing: border-box;
      "
    >

    <div style="display: flex; gap: 0.5rem; justify-content: flex-end;">
      <button
        onclick="closeChangePassword()"
        style="
          background: none;
          border: 1px solid var(--border, #2a2a45);
          color: var(--muted, #888899);
          padding: 0.5rem 1rem;
          border-radius: 8px;
          cursor: pointer;
          font-size: 0.85rem;
        "
      >
        إلغاء / Cancel
      </button>
      <button
        id="cp-submit-btn"
        onclick="submitChangePassword()"
        style="
          background: var(--accent, #C9A84C);
          color: #1a1a2e;
          border: none;
          padding: 0.5rem 1.25rem;
          border-radius: 8px;
          cursor: pointer;
          font-size: 0.85rem;
          font-weight: 700;
        "
      >
        حفظ / Save
      </button>
    </div>

  </div>
</div>


<!-- ═══════════════════════════════════════════════════════════
     Admin Reset Password Modal
════════════════════════════════════════════════════════════ -->
<div
  id="modal-reset-password"
  onclick="if(event.target===this)closeResetPassword()"
  style="
    display: none;
    position: fixed;
    inset: 0;
    background: rgba(0,0,0,0.65);
    z-index: 2000;
    align-items: center;
    justify-content: center;
  "
>
  <div style="
    background: var(--surface, #1a1a2e);
    border: 1px solid var(--border, #2a2a45);
    border-radius: 12px;
    padding: 2rem;
    width: 100%;
    max-width: 400px;
    margin: 1rem;
    box-sizing: border-box;
  ">

    <h3 style="margin: 0 0 0.5rem; color: var(--accent, #C9A84C); font-size: 1rem;">
      🔒 إعادة تعيين كلمة المرور / Reset User Password
    </h3>

    <p
      id="rp-user-name"
      style="font-size: 0.85rem; color: var(--muted, #888899); margin: 0 0 1rem;"
    ></p>

    <div id="rp-error" style="
      display: none;
      background: rgba(224,90,90,0.12);
      border: 1px solid #e05a5a;
      color: #e05a5a;
      border-radius: 8px;
      padding: 0.5rem 0.75rem;
      font-size: 0.85rem;
      margin-bottom: 0.75rem;
    "></div>

    <div id="rp-success" style="
      display: none;
      background: rgba(46,204,138,0.12);
      border: 1px solid #2ecc8a;
      color: #2ecc8a;
      border-radius: 8px;
      padding: 0.5rem 0.75rem;
      font-size: 0.85rem;
      margin-bottom: 0.75rem;
      word-break: break-all;
    "></div>

    <label style="font-size: 0.8rem; color: var(--muted, #888899); display: block; margin-bottom: 0.3rem;">
      New Password / كلمة المرور الجديدة
    </label>
    <input
      type="text"
      id="rp-new"
      placeholder="Min 8 characters"
      style="
        width: 100%;
        background: var(--bg, #0f0f1a);
        border: 1px solid var(--border, #2a2a45);
        color: var(--text, #e8e8f0);
        border-radius: 8px;
        padding: 0.6rem 0.75rem;
        font-size: 0.9rem;
        margin-bottom: 0.5rem;
        outline: none;
        direction: ltr;
        box-sizing: border-box;
      "
    >

    <button
      onclick="generateResetPassword()"
      style="
        background: none;
        border: 1px solid var(--border, #2a2a45);
        color: var(--muted, #888899);
        padding: 0.3rem 0.75rem;
        border-radius: 6px;
        cursor: pointer;
        font-size: 0.8rem;
        margin-bottom: 1.25rem;
        display: block;
      "
    >
      🎲 توليد تلقائي / Auto-generate
    </button>

    <div style="display: flex; gap: 0.5rem; justify-content: flex-end;">
      <button
        onclick="closeResetPassword()"
        style="
          background: none;
          border: 1px solid var(--border, #2a2a45);
          color: var(--muted, #888899);
          padding: 0.5rem 1rem;
          border-radius: 8px;
          cursor: pointer;
          font-size: 0.85rem;
        "
      >
        إلغاء / Cancel
      </button>
      <button
        id="rp-submit-btn"
        onclick="submitResetPassword()"
        style="
          background: var(--accent, #C9A84C);
          color: #1a1a2e;
          border: none;
          padding: 0.5rem 1.25rem;
          border-radius: 8px;
          cursor: pointer;
          font-size: 0.85rem;
          font-weight: 700;
        "
      >
        إعادة التعيين / Reset
      </button>
    </div>

  </div>
</div>