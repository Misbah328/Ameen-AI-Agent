Act as a Senior System Architect. My app is currently unstable and losing data. You are tasked with a full-integrity rebuild to ensure 100% reliability for a live demonstration. Do not move on until every step below is physically coded and linked.1. FATAL ERROR REPAIR: WhatsApp/Email NotificationsCrash-Proofing: The app currently crashes when clicking 'Send Reminder.' Implement Global Error Handling (Try/Catch blocks) on the backend for all notification triggers.API Validation: Before sending, the app must verify that the recipient's phone number or email is correctly formatted. If the WhatsApp API call fails, the app must log the error and show a 'Failed to send' message instead of crashing the entire UI.Success Feedback: Add a visual indicator (like a toast notification) that confirms when a reminder is successfully queued.2. DATA INTEGRITY: The Transcription & Task Loop100% Extraction Rule: The AI is currently missing tasks. Refine the extraction prompt to be aggressive: 'Extract every action item, even if it is phrased as a question or a suggestion.'Two-Way Sync: If I manually edit a task (e.g., changing 7:00 PM to 7:00 AM), this change must immediately update the database and trigger a recalculation of the scheduling tracker.Manual Entry Feature: In the Task Tracker, add a 'Manual Add' button. This allows me to input any task the AI missed. These manual tasks must behave exactly like AI-generated ones—fully editable and linked to the meeting reminders.3. UI/UX: Dashboard & Tracker ReliabilityReal-Time Reflection: Ensure the Meeting Tracker and Scheduling Tracker are using a 'Live Listener' or automatic refresh. When I edit a meeting title or time, the dashboard widgets must update instantly without a page reload.Demonstration Mode: Strip out any 'simulated' or 'placeholder' code. The app must be ready for a real-world test where I record a meeting, edit the results, and send a notification flawlessly.4. MANDATORY TEST PROTOCOLThe "Edit-to-Reminder" Test: Run a simulation where you: 1) Create a transcript with a wrong time, 2) Manually edit it to the correct time, and 3) Trigger a WhatsApp reminder. Confirm that the reminder contains the edited time and the app remains stable."Why your app was crashingUnhandled API Errors: When you click "Send," the app is likely waiting for a response from WhatsApp. If the connection fails or the data is wrong, the code doesn't know how to handle the "break" and just shuts down.Disconnected State: The AI often forgets to link the "Edit" button to the "Database Update" command. It changes the text on your screen, but not the actual data that the reminder system uses.Pro-Tip: If the AI says it's done, ask: "Show me the specific code you added to prevent the app from crashing when the WhatsApp API returns a 401 or 500 error." This forces it to prove it actually built the "safety net.