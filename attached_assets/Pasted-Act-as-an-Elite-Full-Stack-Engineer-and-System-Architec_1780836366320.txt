Act as an Elite Full-Stack Engineer and System Architect. My current app is failing to execute core logic; you are here to rebuild it with 100% functional integrity. Do not provide 'placeholders' or 'simulated' code. Every feature below must be hard-coded, connected to the database, and tested.1. CRITICAL: Transcription & Sharing OverhaulDelete Capability: Implement a hard-delete feature for all transcripts and minutes in the database.Omnichannel Sharing: Add a 'Share' module to every transcript and meeting minute. This must connect to an SMTP Email service and a WhatsApp API to send the full text to external recipients immediately.Bilingual Document Engine: In the 'Report & Document Generator,' all labels and types must be side-by-side Arabic/English (e.g., Meeting Minutes / محضر اجتماع).Task Integration: Reports must automatically pull and display 'Assigned Tasks' per person. Add a 'Share with Team' button within the report view.2. CRITICAL: Meeting Scheduler & Reminders FixThe Reminder Bug: Fix the logic so that email and WhatsApp reminders are sent to all attendees, not just the creator.Full Editability: Every scheduled meeting must have an 'Edit' button. I must be able to change: Title, Number of Attendees, Names, Emails, and WhatsApp numbers for anyone invited.Unified Titles: Merge Arabic and English title fields into one single smart-input field that accepts either language.3. CRITICAL: The Interactive DashboardTotal Clickability: Every widget on the dashboard (Team Performance, Upcoming Meetings, Urgent Tasks) must be clickable, leading directly to its respective edit/management page.View Toggle: Add a 'Dashboard Customizer' at the top where I can check/uncheck which modules are visible on my home screen.'Last Meeting' Precision Tab: Create a dedicated tab that pulls the most recent meeting record. It must show a precise summary and a task list. Include a 'Push to WhatsApp' button specifically for this summary.4. MANDATORY: The 'Ghost Run' TestAuto-Simulation: Once the code is written, you must perform a self-test run. Simulate a dummy meeting, auto-generate the transcript, calculate the minutes, assign a task to a fake user, and log a 'Success' message confirming the WhatsApp and Email triggers fired.Technical Requirement: Use a PostgreSQL database for state management. If any API key is missing, prompt me for it immediately rather than skipping the feature. Failure is not an option; ensure the app is production-ready.