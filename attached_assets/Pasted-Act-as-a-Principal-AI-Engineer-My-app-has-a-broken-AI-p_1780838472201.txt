Act as a Principal AI Engineer. My app has a broken AI pipeline: buttons exist, but the data does not flow correctly. You must rebuild the logic to ensure absolute accuracy in transcription, task extraction, and scheduling.1. REPAIR: The Transcription-to-AI PipelineData Passthrough: After a meeting is transcribed, do not just save the text. Immediately pass the entire transcript to a hidden 'Processing Agent.'Extraction Accuracy: Use a specialized prompt for the AI to: 1) Identify every clear action item, 2) Assign a 'Responsible Person,' and 3) Extract specific dates. If the AI is unsure, it must flag the task as 'Pending Review' rather than ignoring it.Database Sync: Ensure that extracted tasks are automatically written to the Tasks table in the database and linked to the specific Meeting_ID.2. REPAIR: Proactive Scheduling LogicIntent Recognition: When the transcription contains phrases like 'let's meet next Tuesday' or 'schedule a follow-up,' the AI must automatically create a 'Draft Meeting' in the dashboard.Conflict Checking: The scheduling tool must actually check existing database records for time overlaps before allowing a meeting to be finalized.The Reminder Trigger: Fix the background worker. Every time a meeting is 'Confirmed,' a background job must be scheduled to trigger WhatsApp/Email reminders exactly 15 minutes before the start time.3. MANDATORY: The 'Deep Log' DebuggerStep-by-Step Logging: Add a hidden console or log file that tracks exactly what the AI 'sees' from the transcript. If it fails to extract a task, the log must show why (e.g., 'No actionable keywords found').Self-Verification: After you finish the code, run a simulation: Upload a dummy 1-minute transcript containing 3 tasks and 1 meeting request. Verify that 3 tasks and 1 meeting appear in the database. Do not tell me it is done until this test passes."Why this prompt worksStructured Output: It forces the AI to use structured data (like JSON) to move information between the transcript and the task list, which is more reliable than "reading" text.Chain of Thought: It instructs the AI to "think step-by-step" about identifying roles and dates, which significantly improves extraction accuracy.Explicit Tools: It defines the "Processing Agent" as a tool with a clear signature, preventing the "dumb agent" syndrome where the AI forgets its purpose