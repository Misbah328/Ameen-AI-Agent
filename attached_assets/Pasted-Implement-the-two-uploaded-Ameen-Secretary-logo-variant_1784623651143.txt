Implement the two uploaded Ameen Secretary logo variants across the entire application.

Logo usage rules:

1. Light-mode logo
- Use the dark-green and gold logo designed for white or light backgrounds.
- Display it on:
  - landing page
  - login/signup pages
  - light headers
  - white dashboards
  - emails
  - reports
  - exported documents
  - printable pages

2. Dark-mode logo
- Use the white and gold logo designed for dark backgrounds.
- Display it on:
  - dark sidebar
  - dark navigation
  - dark-mode pages
  - mobile drawer
  - splash/loading screens with dark backgrounds

Requirements:
- Replace every old logo reference across the landing page, dashboard, authentication pages, sidebar, mobile navigation, settings, reports, documents, and all other modules.
- Do not leave any old Ameen logo in the application.
- Use one reusable Logo component/helper instead of hard-coding image paths on every page.
- The logo component should automatically select the correct variant based on:
  - light or dark theme
  - background context
  - collapsed or expanded sidebar
- Add supported variants:
  - full horizontal light
  - full horizontal dark
  - icon-only light
  - icon-only dark
- Use transparent PNG/SVG assets with no visible background box.
- Preserve the logo’s aspect ratio.
- Do not stretch, crop, blur, or distort it.
- Keep consistent sizing and spacing throughout the app.
- Add accessible alt text: “Ameen Secretary”.
- On a collapsed sidebar, show only the A icon.
- On an expanded sidebar, show the full logo.
- Ensure the correct logo switches immediately when theme mode changes.
- Avoid duplicating the logo file or loading an incorrect version from cache.

After implementation:
1. Test landing page in light and dark mode.
2. Test login/signup pages.
3. Test dashboard and sidebar.
4. Test collapsed and expanded navigation.
5. Test desktop, tablet, and mobile.
6. Test exported reports and documents if they include branding.
7. Search the project for old logo filenames and remove or replace every remaining reference.
8. Report all files changed and where each logo variant is used.

Do not redesign the logo. Use the two uploaded logo files exactly as the official light and dark variants.