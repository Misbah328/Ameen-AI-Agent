Act as a Senior AI Architect. My app has three critical failures that you must fix immediately to make it 100% production-ready. Do not use placeholders; write final, tested logic for each point.1. REPAIR: Accent & Language IntelligenceThe Global Accent Fix: The transcription engine is failing to grasp Indian, Arabian (Saudi), and mixed accents. You must update the transcription call (e.g., OpenAI Whisper) to include an 'Initial Prompt' that explicitly mentions these accents.Bilingual Detection: If the audio contains both Arabic and English, use a 'Two-Pass' strategy: first detect the language, then transcribe with a multi-language context.Phonetic Matching: For proper nouns (names, places) common in these regions, implement a phonetic correction layer that checks the transcript against my 'Attendees' list to fix misspellings automatically.2. REPAIR: The 'Infinite' Transcription PipelineStop the Cut-off: The transcription currently stops after 2-3 sentences. You must fix the VAD (Voice Activity Detection) and Endpointing logic. Set the silence threshold (endpointing) to at least 2000ms so it doesn't stop during natural pauses.Chunking Logic: Rebuild the frontend to handle 'Streamed' audio. Ensure the transcriptions are appended to a persistent database record in real-time, not just shown in a temporary UI variable that disappears.3. REPAIR: WhatsApp & Reminder LogicThe WhatsApp Fix: The app crashes on 'Send Reminder.' Implement Async Error Handling. If the API (Twilio/WhatsApp) fails, the app must stay alive and show a clear error message (e.g., 'Invalid Number Format' or 'API Key Missing').Full Feature Integration: Ensure the 'Send' button is correctly linked to the latest edited version of the meeting details. If I changed a time from 7 PM to 7 AM, the WhatsApp reminder must send the 7 AM update.4. MANDATORY: The 'Demonstration' Stress TestSelf-Verification: Before finishing, simulate a 5-minute meeting with long pauses and a mix of English and Arabic terms.Verification Checklist:Did the transcription continue past 10 sentences?Did it correctly transcribe regional names?Did clicking 'Send WhatsApp' result in a success/fail log without a crash?System Requirement: Update the .replit config and Secrets to ensure all API keys for transcription and WhatsApp are correctly loaded."Why this prompt will work for youAccent Support: By giving the AI an "Initial Prompt" during transcription (a technical feature of models like Whisper), it primes the AI to expect specific speech patterns.No More Truncation: Setting a 2000ms endpointing limit prevents the AI from thinking the meeting is over just because someone took a breath.Crash Prevention: It forces the AI to build "safety nets" (Try/Catch blocks) so that one bad phone number doesn't break your entire application.